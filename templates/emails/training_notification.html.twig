<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{{ isEnrolled ? 'Confirmation d\'inscription' : 'Confirmation de désinscription' }}</title>
</head>
<body>
    <p>Bonjour {{ user.email }},</p>
    
    {% if isEnrolled %}
        <p>Vous êtes bien inscrit(e) à la formation <strong>{{ training.title }}</strong>.</p>
    {% else %}
        <p>Vous vous êtes désinscrit(e) de la formation <strong>{{ training.title }}</strong>.</p>
    {% endif %}

    <p><strong>Date de début :</strong> {{ training.startDate | date('d/m/Y') }}</p>

    <p>Merci et à bientôt !</p>
</body>
</html>
