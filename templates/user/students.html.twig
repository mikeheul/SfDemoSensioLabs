{% extends 'base.html.twig' %}

{% block title %}Students{% endblock %}

{% block body %}
    <div class="container max-w-7xl mx-auto py-8">
        <h1 class="text-5xl font-bold text-gray-800 mb-8 flex items-center">
            Students 
            <span class="ml-2 inline-block py-1 px-3 text-white bg-indigo-600 rounded-full text-lg">
                {{ students | length }}
            </span>
        </h1>

        {% if students is empty %}
            <p class="text-lg text-gray-600">No students found.</p>
        {% else %}
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                {% for student in students %}
                    {% include 'components/user/user_card.html.twig' with { 'user': student, 'link_route': 'app_student_show' } %}
                {% endfor %}
            </div>
        {% endif %}
    </div>
{% endblock %}
