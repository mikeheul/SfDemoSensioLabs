<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Confirmation d'inscription</title>
    <style>
        body { font-family: Arial, sans-serif; font-size: 14px; }
        .container { padding: 20px; border: 1px solid #ddd; }
        h1 { color: #333; }
        p { margin-bottom: 10px; }
        .footer { margin-top: 20px; font-size: 12px; color: #777; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Confirmation d'inscription</h1>
        <p>Bonjour {{ user.email }},</p>
        <p>Vous êtes inscrit(e) à la formation :</p>
        
        <h2>{{ training.title }}</h2>
        <p><strong>Date :</strong> {{ training.startDate|date('d/m/Y') }}</p>
        <p><strong>Description :</strong> {{ training.description }}</p>

        <h2>Programme</h2>
        {% for course in training.courses %}
            - {{ course.name }} <br>
        {% endfor %}

        <p>Merci de votre confiance.</p>
        <p>L'équipe de Formation</p>

        <div class="footer">
            <p>Formation.com | Contact : support@formation.com</p>
        </div>
    </div>
</body>
</html>
