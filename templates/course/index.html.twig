{% extends 'base.html.twig' %}

{% block title %}Courses List{% endblock %}
{% block meta_description %}
    <meta name="description" content="Courses Page - List of all available courses.">
{% endblock %}

{% block body %}
    <div class="container max-w-7xl mx-auto py-8">
        {% include 'components/ui/page_title.html.twig' with { 'title': 'page_title.courses', 'count': courses | length } %}

        <div class="bg-indigo-100 p-6 mb-8 rounded-lg">
            <p class="text-gray-700">
                {{ 'page_description.courses'|trans }}
            </p>
        </div>

        <div class="mb-6">
            {% include 'components/ui/create_button.html.twig' with { 'route': 'new_course', 'label': 'Create New Course' } %}
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
            {% for course in courses %}
                {% include 'components/course/course_card.html.twig' %}
            {% endfor %}
        </div>
    </div>
{% endblock %}
